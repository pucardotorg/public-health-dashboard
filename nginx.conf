server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Vite is built with base "/public-health-dashboard/", so every asset URL
    # in index.html is prefixed with that path. Serve the app under the same
    # prefix (the ingress path also matches) and fall back to its index.html
    # for client-side routes.
    location /public-health-dashboard/ {
        try_files $uri $uri/ /public-health-dashboard/index.html;
    }

    location ~* ^/public-health-dashboard/.*\.(?:css|js|svg|png|jpg|jpeg|gif|ico|woff2?)$ {
        expires 30d;
        access_log off;
        add_header Cache-Control "public";
    }
}
